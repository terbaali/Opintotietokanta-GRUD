<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>Edit opiskelija</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

	<% if (messages.error) { %>
	<div class="alert alert-danger" role="alert"><%- messages.error %></div>
	<% } %>

    <div class="card"> 
    	<div class="card-header">
			Muokkaa opiskelijaa
		</div>
        <div class="card-body">
            <form action="/opiskelija/update/<%= idopiskelija %>" method="post">
                <div class="form-group">
                    <label>etu_nimi:</label>
                    <input type="text" class="form-control" name="etu_nimi" value="<%= etu_nimi %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>suku_nimi:</label>
                    <input type="text" class="form-control" name="suku_nimi" value="<%= suku_nimi %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>luokka_tunnus:</label>
                    <input type="text" class="form-control" name="luokka_tunnus" value="<%= luokka_tunnus %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>osoite:</label>
                    <input type="text" class="form-control" name="osoite" value="<%= osoite %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-info" value="Päivitä"/>
                </div>
            </form>
        </div>
    </div>

</body>
</html>